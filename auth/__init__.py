"""Multi-user auth for the TELEMAC viewer.

Spec: docs/superpowers/specs/2026-05-08-user-accounts-design.md

Public API is re-exported at the end of Task 9 once all submodules
are in place. Until then, callers must `from auth.core import ...`,
`from auth.middleware import ...`, etc. directly.
"""
